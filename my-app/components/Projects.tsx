import React from "react";
import Card from "../components/Card"

type Props = {};

const Projects = (props: Props) => {
  return (
    <div className="h-screen">
      <h2 className="text-red-500 text-7xl">Project page</h2>
    </div>
  );
};

export default Projects;
